#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tilelive/bin/copy" "$@"
  ret=$?
else 
  node  "$basedir/../tilelive/bin/copy" "$@"
  ret=$?
fi
exit $ret
