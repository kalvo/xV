#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mbtiles/bin/mbpipe" "$@"
  ret=$?
else 
  node  "$basedir/../mbtiles/bin/mbpipe" "$@"
  ret=$?
fi
exit $ret
